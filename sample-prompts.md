# 参考提示词

这些不是命令，是措辞参考。路由规则本身在 `~/.claude/CLAUDE.md` 的受管块里，主对话每次会话都加载着——你只要说清目标和边界，它会自己决定用哪几个 agent。

## 安装自检

```text
总结当前生效的工程团队路由规则、交接契约，以及可用的自定义 agent 及其配置的 model 与 effort。
说明你加载了哪些全局与项目指令文件。不要编辑文件，也不要派生任何 subagent。
```

## 完整特性流程

```text
实现 <特性>。每次交接都要保住原始需求。

用最少的必要角色：路径不清楚才用 explorer；只有跨越真实架构边界才用 architect；
实现前起一个全新的 reviewer 走 ARCHITECTURE；worker 严格按实现包做，
只有写面不重叠的包才并行；tester 做独立行为覆盖；
宣布完成前起一个全新的 reviewer 走 CODE+ACCEPTANCE。

每条 CHANGES_REQUIRED 都按 owner 路由回去解决。不要提交。
```

## 小缺陷修复

```text
修复 <缺陷>。流程按风险裁剪：责任路径不清楚才用 explorer；worker 做最小修正；
tester 补一个高价值回归测试；只有存在实质风险才用 reviewer。

一次聚焦修正或两轮验证失败之后升级到 debugger，不要靠猜测性编辑硬试。不要提交。
```

## 纯架构任务

```text
分析并设计 <变更>。只收集真正需要的仓库事实，然后让 architect 给出最小的持久决策和有边界的实现包。

起一个全新的 reviewer 走 ARCHITECTURE，挑战过度设计、状态归属、失败行为、迁移风险，
以及是否存在实质上更简单的方案。

返回修订后的设计与交接。不要改代码。
```

## 疑难调试

```text
排查 <失败现象>。保留所有已有证据。本地原因不明显、或常规尝试已经失败，才用 debugger。

按顺序要求：复现或最强可得证据 → 逐条验证过的假设 → 已证明的根因 → 最小修正 →
聚焦验证 → 最后起一个全新的 reviewer。

拒绝用重试、sleep、宽泛 catch 或防御层把失败盖过去的修法。不要提交。
```

## 终审后提交

```text
实现已通过全新的 CODE+ACCEPTANCE 评审。用 git-operator 检查 git status 和真实 diff，
只暂存预期内的文件，按仓库近期的提交信息约定生成一个聚焦提交。

报告提交哈希和剩余工作区状态。不要推送。
```
